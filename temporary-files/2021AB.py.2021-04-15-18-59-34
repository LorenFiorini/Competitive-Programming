import numpy

def count(mat, R, C):
	prefix = [[0] * R] * C 
	
	for i in range(R):
		for j in range(C):
			




def solve(_):
	R, C = input().split()
	R = int(R)
	C = int(C)
	mat = list()
	for i in range(R):
		row = list(map(int, input().split()))
		mat.append(row)
	
	ans = 0
	count(mat, R, C, ans)
	
	
	
	
	print("Case #{}: {}".format(_, ans))


def main():
	t = int(input())
	for _ in range(1, t + 1):
		solve(_)
	




if __name__ == '__main__':
	main()

